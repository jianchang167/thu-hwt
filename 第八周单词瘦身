#include<iostream>
#include<string>
using namespace std;

int main() 
{
	string a;
	cin >> a;
	int n = size(a);
	cout << a << endl;
	for (int i = 0; i < n; i++)
	{
		a = a.substr(0, n - i - 1);
		cout << a << endl;
		}

}
